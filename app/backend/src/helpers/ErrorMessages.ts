enum ErrorMessages {
  INVALID_ID_ERROR = 'Id must have 24 valid hexadecimal characters',
  PAYLOAD_INCORRECT = "Data received isn't the expected shape",
  OBJECT_NOT_EXIST = 'Requested ID not exist on database'
}

export default ErrorMessages;
